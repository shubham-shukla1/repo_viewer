//codec is not a part of any certain layer, create shared under core, which do not fit into any layer
//it can be used from any feature thats why under core
//core is feature of all feature, its also need to be separated
import 'dart:convert';

final stringToBase64 = utf8.fuse(base64);
